# dependencies
node_modules/

# next.js / build output
.next/
out/
dist/
build/
*.tsbuildinfo
# Next regenerates this on every build; tracking it leaves a dirty tree
next-env.d.ts

# env / secrets
.env
.env*.local

# supabase local
supabase/.branches/
supabase/.temp/

# brainstorm session state + mockups (transient)
.superpowers/

# machine-specific skill symlinks (recreated via `npx skills`)
.claude/skills/

# knowledge-mining: source-specific target manifests + raw dumps (brand-bearing, never commit)
*.local.txt
tools/knowledge-mining/.raw/

# logs / OS
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Brand/personnel-bearing reference PDFs dropped in docs/ — mine abstractly, never commit (IP guardrail)
docs/*.pdf
